spring:
  application:
    name: project-service
  config:
    import: optional:configserver:http://localhost:8888
server:
  port: 8080

#actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"


  endpoint:
    health:
      show-details: always

  info:
    env:
      enabled: true

info:
  app:
    name: roczyno
